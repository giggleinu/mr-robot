# Mr Robot


## Installation

1. Clone the repo `https://github.com/giggleinu/mr-robot.git`
2. Install packages using `yarn install`
3. Run the app in dev using `yarn start`. This should open [http://localhost:3000](http://localhost:3000) in the browser.
